<%- include("templates/cabecera",{titulo:"Usuarios"}) %>

<div class="container">
    <h1>Registro usuarios</h1>

    <a class="btn btn-success my-4" href="/usuarios/crear">
        Crear nuevo usuario
    </a>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Correo</th>
                <th>Rol</th>
                <th>Acción</th>
            </tr>
        </thead>
        <tbody>
            <% arrayUsuarios.forEach(usuario => { %>
                <tr>
                    <td><%= usuario._id %></td>
                    <td><%= usuario.nombre %></td>
                    <td><%= usuario.correo %></td>
                    <td><%= usuario.rol %></td>
                    <td>
                        <a class="btn btn-warning btn-sm"
                           href="/usuarios/<%= usuario._id %>">
                            Editar / Eliminar
                        </a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>

<%- include("templates/cierre") %>
